# Placeholder Secret matching 003_grants.sql per-pod role password.
# TODO Phase 4: replace via External Secrets Operator + ClusterSecretStore -> AWS Secrets Manager (bookflow/rds/forecast-svc).
apiVersion: v1
kind: Secret
metadata:
  name: forecast-svc-secret
  namespace: bookflow
type: Opaque
stringData:
  FORECAST_RDS_PASSWORD: CHANGE_ME_FORECAST
  # Optional: only needed when the GCP Cloud Function requires an externally
  # supplied bearer token. Prefer private networking/IAM-bound invocation.
  FORECAST_GCP_FUNCTION_BEARER_TOKEN: ""
  AUTH_JWT_SIGNING_KEY: REPLACE_ME_JWT
