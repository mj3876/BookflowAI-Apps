# Placeholder Secret matching 003_grants.sql per-pod role password.
# TODO Phase 4: replace via External Secrets Operator + ClusterSecretStore -> AWS Secrets Manager (bookflow/rds/decision-svc).
apiVersion: v1
kind: Secret
metadata:
  name: decision-svc-secret
  namespace: bookflow
type: Opaque
stringData:
  DECISION_RDS_PASSWORD: CHANGE_ME_DECISION
  AUTH_JWT_SIGNING_KEY: REPLACE_ME_JWT
