apiVersion: v1
kind: Service
metadata:
  name: decision-svc
  namespace: bookflow
spec:
  type: ClusterIP
  selector:
    app: decision-svc
  ports:
    - name: http
      port: 80
      targetPort: http
