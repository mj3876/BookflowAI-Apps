apiVersion: v1
kind: Service
metadata:
  name: intervention-svc
  namespace: bookflow
spec:
  type: ClusterIP
  selector:
    app: intervention-svc
  ports:
    - name: http
      port: 80
      targetPort: http
