# Placeholder Secret matching 003_grants.sql per-pod role password.
# TODO Phase 4: replace via External Secrets Operator + ClusterSecretStore -> AWS Secrets Manager (bookflow/rds/intervention-svc).
apiVersion: v1
kind: Secret
metadata:
  name: intervention-svc-secret
  namespace: bookflow
type: Opaque
stringData:
  INTERVENTION_RDS_PASSWORD: CHANGE_ME_INTERVENTION
  AUTH_JWT_SIGNING_KEY: REPLACE_ME_JWT
