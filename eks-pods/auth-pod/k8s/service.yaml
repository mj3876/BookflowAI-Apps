apiVersion: v1
kind: Service
metadata:
  namespace: bookflow
  name: ${POD_NAME}
spec:
  type: ClusterIP
  selector: { app: ${POD_NAME} }
  ports:
    - name: http
      port: 80
      targetPort: 80
