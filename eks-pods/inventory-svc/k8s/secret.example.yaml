# Placeholder Secret matching 003_grants.sql per-pod role password.
# TODO Phase 4: replace via External Secrets Operator + ClusterSecretStore -> AWS Secrets Manager (bookflow/rds/inventory-svc).
apiVersion: v1
kind: Secret
metadata:
  name: inventory-svc-secret
  namespace: bookflow
type: Opaque
stringData:
  INVENTORY_RDS_PASSWORD: CHANGE_ME_INVENTORY
  AUTH_JWT_SIGNING_KEY: REPLACE_ME_JWT
