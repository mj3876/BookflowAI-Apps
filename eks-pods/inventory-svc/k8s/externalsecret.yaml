# RDS password + JWT signing key from AWS Secrets Manager via ESO.
# eks_addons 가 ESO + ClusterSecretStore `bookflow-aws-secrets` 를 깔아둠.
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: inventory-svc-secret
  namespace: bookflow
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: bookflow-aws-secrets
    kind: ClusterSecretStore
  target:
    name: inventory-svc-secret
    creationPolicy: Owner
  data:
    - secretKey: INVENTORY_RDS_PASSWORD
      remoteRef:
        key: bookflow/rds/master-password
        property: password
    - secretKey: AUTH_JWT_SIGNING_KEY
      remoteRef:
        key: bookflow/auth/jwt-signing-key
