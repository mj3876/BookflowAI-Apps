apiVersion: v1
kind: Service
metadata:
  name: notification-svc
  namespace: bookflow
spec:
  type: ClusterIP
  selector:
    app: notification-svc
  ports:
    - name: http
      port: 80
      targetPort: http
