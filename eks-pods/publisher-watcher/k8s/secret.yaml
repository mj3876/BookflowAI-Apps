# Placeholder Secret matching 003_grants.sql per-pod role password.
# TODO Phase 4: replace via External Secrets Operator + ClusterSecretStore -> AWS Secrets Manager (bookflow/rds/publisher-watcher).
apiVersion: v1
kind: Secret
metadata:
  name: publisher-watcher-secret
  namespace: bookflow
type: Opaque
stringData:
  PUBWATCH_RDS_PASSWORD: CHANGE_ME_PUBLISH
